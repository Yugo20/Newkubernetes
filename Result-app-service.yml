---
apiVersion: v1
kind: service
metadata:
  name: result-app-service
  labels:
    author: yugo
spec:
  type: LoadBalancer
  ports:
    - targetPort: 80
      port: 80
  selector:
    name: result-app-deployment
...